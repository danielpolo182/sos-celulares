import type { ReactNode } from 'react'

export const metadata = { title: 'Inbox WhatsApp' }

export default function WhatsAppLayout({ children }: { children: ReactNode }) {
  return <>{children}</>
}
